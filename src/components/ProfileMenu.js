import React from "react";

const ProfileMenu = () => {
  // Add any necessary functionality for the profile menu items

  return (
    <div className="profile-menu">
      <button>Profile</button>
      <button>Settings</button>
    </div>
  );
};

export default ProfileMenu;
